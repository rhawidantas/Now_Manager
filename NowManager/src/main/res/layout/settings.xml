<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_height="fill_parent"
        android:layout_width="fill_parent">

    <PreferenceCategory android:title="General">

        <!--<CheckBoxPreference
            android:title="Audio Responsive"
            android:summary="Snap or clap to add a new log (plays your text tone as feedback)."
            android:key="audioResponse"
            android:defaultValue="false"/>

        <ListPreference
            android:title="Audio Sensitivity"
            android:key="audio_sensitivity"
            android:entries="@array/sensitivity_levels"
            android:entryValues="@array/sensitivity_values"
            android:dependency="audioResponse"
            android:summary="Must have the above setting enabled."
            android:defaultValue="6"/>-->

        <CheckBoxPreference
            android:title="Volume Keys"
            android:summary="Use the volume up or down buttons to add a new log."
            android:key="volumeKeys"
            android:defaultValue="false"/>

        <CheckBoxPreference
            android:title="Haptic Feedback"
            android:summary="Vibrate when a new log is added."
            android:key="vibrateOn"
            android:defaultValue="false"/>

    </PreferenceCategory>

    <!--<PreferenceCategory android:title="Savings Logs">

        <Preference
            android:key="exportDatabase"
            android:title="Export"
            android:summary="Save all logs to storage."/>

    </PreferenceCategory>-->


</PreferenceScreen>